FROM python:3.12-slim AS base
WORKDIR /app

COPY apps/anomaly-engine/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY apps/anomaly-engine/ .

EXPOSE 8000
CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000"]
